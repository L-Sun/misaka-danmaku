syntax = "proto3";

message Response {
    oneof response {
        StoreResponse     store     = 1;
        FindNodeResponse  findNode  = 2;
        FindValueResponse findValue = 3;
    }
}

message StoreResponse {
    enum State {
        SUCCESS = 0;
        FAILED  = 1;
    }
    State state = 1;
}

message FindNodeResponse {
    message NodeInfo {
        bytes  id      = 1;
        bytes  address = 2;
        uint32 port    = 3;
    }
    repeated NodeInfo nodes = 1;
}

message FindValueResponse {
    message Data {
        bytes key   = 1;
        bytes value = 2;
    }

    repeated Data data = 1;
}